"""Расчётный движок инвестиционного дозора. Код монтируется в сайдкар invest-calc из ./calc/engine.

Правила: детерминизм (seed обязателен и записывается), версия у каждой модели, LLM не считает —
только вызывает и интерпретирует. Модели регистрируются в registry.MODELS.
"""
